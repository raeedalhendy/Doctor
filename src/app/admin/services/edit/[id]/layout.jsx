export const metadata = {
    title: "Edit Service",
    description: "Edit Service next app",
  };
  
  export default function EditServicelayout({ children }) {
    return (
      <div>{children}</div>
    );
  }
  