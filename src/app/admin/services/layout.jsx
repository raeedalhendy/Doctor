export const metadata = {
    title: "Services",
    description: "Services next app",
  };
  
  export default function Serviceslayout({ children }) {
    return (
      <div className="h-screen overflow-auto">{children}</div>
    );
  }
  