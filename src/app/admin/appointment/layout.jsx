export const metadata = {
    title: "المواعيد",
    description: "Login next app",
  };
  
  export default function appointmentlayout({ children }) {
    return (
      <div>{children}</div>
    );
  }
  