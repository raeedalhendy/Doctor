export const metadata = {
    title: "Edit Patients",
    description: "Edit Patients next app",
  };
  
  export default function EditServicelayout({ children }) {
    return (
      <div>{children}</div>
    );
  }
  