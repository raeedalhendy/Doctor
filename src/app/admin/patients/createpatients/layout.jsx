export const metadata = {
    title: "CreatePatients",
    description: "CreatePatients next app",
  };
  
  export default function CreatePatientslayout({ children }) {
    return (
      <div >{children}</div>
    );
  }
  